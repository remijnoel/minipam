{
  "name": "minipam-ui",
  "version": "1.0.0",
  "description": "MiniPAM Web UI",
  "main": "dist/app.js",
  "scripts": {
    "build": "npm run copy",
    "copy": "mkdir -p dist && cp public/index.html dist/ && cp src/app.js dist/",
    "dev": "python -m http.server 8080 --directory dist",
    "clean": "rm -rf dist/*"
  },
  "dependencies": {},
  "devDependencies": {},
  "keywords": ["ipam", "vue", "network", "cidr"],
  "author": "MiniPAM Contributors",
  "license": "MIT"
}